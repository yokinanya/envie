Envie 的配置大部分都可以通过 CLI 管理。参见 `envie config --help`。

本地配置保存在 `config.json` 文件中，这个文件位于你的操作系统标准配置目录下：

- Linux：`$XDG_CONFIG_HOME`*/envie*
- macOS：*~/Library/Application Support/envie*

你也可以通过环境变量 `ENVIE_CONFIG_DIRECTORY` 覆盖这个位置。
